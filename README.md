# TECHSHOP MANAGEMENT FRONTEND (React)

To run the project, you need to install the dependencies first:

```
npm install
```

Then, you can run the project with:

```
npm start
```

The project will be running on http://localhost:3000
